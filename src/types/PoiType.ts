export default interface PoiType {
  id: number;
  azure_tag: string;
  exhibition_number: number;
  title: string;
  author: string;
  periode: string;
  visible: boolean;
  description: string;
  origin: string;
}
